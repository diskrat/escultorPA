#ifndef LERDEARQUIVO_HPP
#define LERDEARQUIVO_HPP
#include "FiguraGeometrica.hpp"
class lerDeArquivo
{
public:
    lerDeArquivo(const char *filename);
    // ~lerDeArquivo();
};

#endif // LERDEARQUIVO_HPP